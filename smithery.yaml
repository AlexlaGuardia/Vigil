startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      DB_PATH:
        type: string
        description: "Path to Vigil SQLite database"
        default: "vigil.db"
    required: []
  commandFunction:
    - vigil
    - serve
